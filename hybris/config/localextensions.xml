<?xml version="1.0" encoding="ISO-8859-1"?>



<!--
   extensions.xml - hybris Platform

   Note: If a file localextensions.xml is available, it is used instead of this file.

   this file configures which hybris Platform extensions are installed,
   each <extension dir=".."> points to an extension directory. The path can be
   absolute or relative.

   After adding an extension, call 'ant' to build the system.
-->

<hybrisconfig xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="resources/schemas/extensions.xsd">

	<extensions>
		<!-- *************************************************** -->
		<!-- *** Platform Modules                            *** -->
		<!-- *************************************************** -->
		<!-- All platform extensions will be included automatically. -->
		


		<!-- *************************************************** -->
		<!-- *** All the others..                            *** -->
		<!-- *************************************************** -->
			
		<!--  		
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/acceleratorcockpits"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/acceleratorcore"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/acceleratorfacades"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/acceleratorsampledata"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/acceleratorstorefront"/>			
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/b2bapprovalprocess"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/b2bcommerce"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/b2bstore"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/basecommerce"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/bmecat"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/btg"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/btgcockpit"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/classificationsystems"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/cms2"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/cms2lib"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/cmscockpit"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/commercefacades"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/commerceservices"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/commercewebservices"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/cscockpit"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/customerreview"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/cybersource"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/flexstore"/>		
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/fulfilmentprocess"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/jms"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/mam"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/mamdemo"/>	
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/mobileoptionals"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/mobileservices"/>	
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/mobiletemplate"/>	
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/oci"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/payment"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/perfweb"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/print"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/printcockpit"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/productcockpit"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/promotions"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/reportcockpit"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/sampledata"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/soapspring"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/solrfacetsearch"/>		
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/springmvcstore"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/storetemplate"/>		
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/testdata"/>	
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/ticketsystem"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/voucher"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/wishlist"/>
		<extension dir="${HYBRIS_BIN_DIR}/ext-hybris/ycockpit"/>
		-->

	</extensions>

</hybrisconfig>
